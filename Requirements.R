# Requirements, run once before single_stock_analysis.R

install.packages(c("tidyverse",
                   "tidyquant",
                   "frenchdata",
                   "broom",
                   "zoo"))